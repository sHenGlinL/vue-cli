import { Module } from 'vuex';
import { State } from '../index';

const state = {

};

export type systemType = typeof state;

export default {
  namespaced: true,
  state,
  mutations: {
    
  },
  actions: {
    
  }
} as Module<systemType, State>;
